{
  "name": "ai-run-ui",
  "version": "1.0.0",
  "description": "Unified project runner for AI-Run frontend + ranking backend",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"cyan,magenta\" \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev",
    "backend": "cd ranking && ..\\..\\.venv\\Scripts\\uvicorn backend.main:app --host 127.0.0.1 --port 8000 --reload",
    "frontend": "npm run dev --prefix frontend",
    "install:backend": "..\\.venv\\Scripts\\pip install fastapi uvicorn pandas pydantic python-dotenv python-multipart pdfplumber groq numpy",
    "install:frontend": "npm install --prefix frontend",
    "install:all": "npm run install:backend && npm run install:frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.3"
  }
}
